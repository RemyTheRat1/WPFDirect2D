namespace WpfDirect2D
{
    public enum ShapeRenderOrigin
    {        
        /// <summary>
        /// Render the shape with the desired location at the center of the shape
        /// </summary>
        Center,

        /// <summary>
        /// Render the shape with the desired location at the top left corner of the shape
        /// </summary>
        TopLeft
    }
}
